{
    "private": true,
    "scripts": {
        "postinstall": "node_modules/getcomposer/composer.phar install && cd public && npm install --unsafe-perm"
    },
    "dependencies": {
        "getcomposer": "1.3.3"
    }
}
